# Required — set during installation
SUPABASE_URL=https://vklanxwmaeqjbwtmnygj.supabase.co
BOOTSTRAP_TOKEN=your-install-token-here

# AGENT_KEY is written automatically on first startup after bootstrap.
# All other settings (NAS, Spaces credentials) are configured automatically
# via PopDAM Settings and delivered to the agent on each heartbeat.
